# D006 — D005 deployed: overlap fixed, modest throughput change

Completed demo deployment and bounded comparison, 2026-09-20. Prepared for **operator manual relay**; no direct cross-task message sent. The developer has recorded D003/D004/D005; D003/D004 recommendations remain historical and are not implemented by this revision.

## Follow-up before relay — 05:21 UTC observation

The later steady rate is **485,832 courts/hour over ten minutes**, with **481,711/hour over five minutes** and **488,314/hour in the latest completed minute** (ending 05:20:30). The ten-minute rate is 4.64% above the initial post-deployment comparison and 6.94% above its pre-deployment baseline. This extends the evidence without replacing the original matched-window result or claiming all variation is caused by code.

A later guarded owner is now confirmed: monitoring changed from PID 1575949 to PID 1575971, whose next aggregate began at 05:13:02. A live check at 05:21 still found one guarded client and four parallel workers. This supersedes the earlier **not-yet-observed subsequent evaluation** limitation below; it does not establish the prior invocation's full outcome from database activity alone.

At the 05:20:30 endpoint, 456,814 / 914,453 courts were done and 457,639 remained. At the latest five-minute workload, that is about 57 minutes remaining, conditional on later courts having similar cost. A **speculative additional 5–15% throughput gain** from bounding the scan would save about 3–8 minutes of this remaining phase before deployment overhead. Zero gain remains possible because the shared audit writer is still a bottleneck. The latest deployment paused the run for 3.44 minutes, so a repeat pause could consume much of Phase 8's isolated saving. The stronger reason to consider the bounded scan is repeated interference across phases 9–11 as well; their time saving cannot yet be responsibly totaled. These are planning scenarios, not a measured performance promise.

## Disposition

**Retain `5a78d6a11c28db2a6c92122c7526fac7d196bcde`.** Its intended execution guard works in the observed live run: 25 unguarded population-sweep clients and 32 parallel workers became one guarded client and four parallel workers. Correctness samples passed. Throughput changed from **454,320 to 464,306 courts/hour (+2.20%)**, a modest observed difference within minute-to-minute variability, not an established substantial speedup.

The surviving CLK-06 query is still expensive and retains an old snapshot. One next recommendation below targets that query's work per statement while retaining this successful guard.

## Review and deployment

- Same run `01a0ba29-3dbb-7181-8654-2d41ce1dea86`, Phase 8 `judiciary`; same completed 914,453-item worklist, 73 simulation workers, 0.1% population sample, 62% turnout and existing aspects.
- Fetched GitHub `main`; reviewed the exact D005 response and runtime/test diff from `e8e0910c`. Only `EvaluateCriticalPopulationJob` changes runtime behavior. Reviewed developer evidence of **10 tests / 108 assertions**, including independent owners/duplicates, failure/death recovery, reconnect fencing, outer-transaction preservation and existing eligibility guards. Incoming and deployed PHP passed syntax checks. No live write fixtures or production test suite was run.
- `sim:halt` requested **05:00:22 UTC**; drained **05:00:28**, with zero running items, zero leases, zero pending counter deltas and zero review. Final retained count was **325,174 done / 589,279 pending**.
- Used the established **`docker stop --time 120 fc_horizon`**, starting 05:01:43.604. The fixed timeout expired; Horizon stopped at **05:03:44.122**, exit 137, not a clean graceful shutdown. No active Step 5 item was interrupted. This was a bounded Horizon refresh, not an indefinite wait.
- After Horizon stopped, 25 old CLK-06 client sessions were still scanning. Captured each PID, backend start and query fingerprint; terminated only sessions still matching those identities, the stopped Horizon address and the specific CLK-06 aggregate. All 25 termination calls succeeded; matching old clients were zero before new workers started. No other database session or independent Claude process was targeted. No queues were purged.
- Fast-forwarded exactly to `5a78d6a1`; all four existing local configuration checksums passed. Horizon restarted at **05:03:46.495**. `sim:resume` and `sim:pump` completed by **05:03:48.119**. Startup and drain are excluded from benchmark windows. The full 73-lease pool was present by the 05:06:30 recorded endpoint.
- PostgreSQL, scheduler and Redis retained their previous start times. Redis still has the temporary 2 GiB container cap and unchanged data/persistence/eviction settings. No migration, frontend build, PostgreSQL restart, scheduler refresh, sizing change, manual ANALYZE or VACUUM occurred. The independent Claude monitoring/storage/shutdown loop was untouched.
- Refreshed Horizon has workers for `sim`, `default` and `long-running`; no supervisor width configuration was changed. Existing queued envelopes were left for ordinary guarded consumption.

## Completed comparison

| Condition | UTC interval | Direct completions | Weighted courts/hour |
|---|---|---:|---:|
| Before: e8e0910c, overlapping sweeps | 04:56:30.739–04:59:30.724 | 22,714 in 179.984 s | **454,320** |
| After: 5a78d6a1, one guarded sweep | 05:06:30.712–05:09:30.710 | 23,215 in 179.998 s | **464,306** |

Post-deployment minute rates: **436,489; 456,019; 500,410**. All endpoints used 73 fresh leases, complete generation, no halt and zero review. The 40 recent item observations captured across each side's endpoints were all newly appointed 5/5-seat courts; these are bounded workload samples, not a proof of identical work throughout both windows. This was sequential live work, not a replay. The treatment includes the required retirement of old scans and worker refresh.

| Timer | Before mean | After mean |
|---|---:|---:|
| Whole item | 583.85 ms | 569.02 ms |
| Stage including audit | 486.18 ms | 511.53 ms |
| Claim acquisition | 75.01 ms | 35.29 ms |
| Audit lock acquisition | 153.07 ms | 198.07 ms |
| Final audit batch | 161.03 ms | 206.08 ms |
| Stage nominations | 121.03 ms | 129.26 ms |
| Seat adopted slate | 56.22 ms | 45.32 ms |
| Resident pools | 2.19 ms | 1.80 ms |

Nested times must not be summed. Worker timing flushes contain 22,250 before whole-item samples and 22,600 after against the direct completion counts; JSON evidence also records total microseconds divided by directly completed scopes. Claim work improved, but increased audit waiting offset much of that improvement. No nomination, voting, seating, audit or ledger implementation changed in this deployment.

## Guard and remaining-query evidence

Across **21 post-resume observations, 05:04:06–05:10:48**, the maximum was one guard owner, one client sweep and four PostgreSQL parallel workers. The owner was PID 1575949. Its scan began **05:03:49.869** and was still active **418.6 seconds later**, usually observed in DataFileRead. It held the oldest snapshot; at 05:10:51 the oldest snapshot was about 2.30 million transaction IDs behind current work. `sim_items` estimated dead tuples reached 816,446; its last autovacuum remained 04:16:32. Delayed cleanup is a plausible consequence of long snapshots, not an isolated measurement of the claim-time cost.

**Later scheduled evaluation is not yet verified live:** the first guarded sweep has not finished in this observation window. Do not call its long duration a failed guard or claim that another legitimate owner has already run. Local tests cover later-run/death recovery; the bounded guard monitor continues recording. No additional sweep was manually dispatched to manufacture a result.

A planner-only EXPLAIN of the complete source query with its initial zero-UUID cursor and 5,000 result limit has `Limit -> Aggregate -> Sort` above anti-joins and a parallel resident index scan. That scan estimates **27,113,674 rows per worker**, with overall plan cost approximately 1.49 million. Estimates are not actual row counts or elapsed measurements. The result LIMIT does not cap source work when nearly all jurisdictions are excluded. No EXPLAIN ANALYZE was run. The session guard itself is not a long transaction: the active aggregate statement retains the snapshot.

Automatic table statistics maintenance was observed at 05:10:30, after the completed post benchmark window; no operator statistics intervention was made.

## Bounded correctness and latest health

A fresh post-deployment 1,000-item sample reported **1,000 appointed five-seat courts / 5,000 judges**, with zero sampled deferrals. Across 20 selected courts, all checks passed for:

- 100 nominations, appointments, active terms, expiry timers, dossiers and certification records with matching court/seat/user/jurisdiction links.
- 20 adopted slate votes and passing quorum/majority tallies.
- Exact ordered first-five active-resident assignment for every sampled court; no duplicate judge within a bench.
- Court counts and creation-law references.

**129 audit hashes and 128 adjacent links passed**; the tail contained 119 court batches and 10 achievement records. Samples were newly completed after deployment. Limits: five-seat committee courts only; no constituent-mode/dry-pool cases or full-world verification.

Latest recorded phase health at **05:10:30 UTC**: **375,836 / 914,453 done**, 538,549 pending, 68 running, 73 fresh leases, zero review and no halt.

## One next bounded recommendation

**Bound the surviving EvaluateCriticalPopulationJob scan by source work before eligibility filtering and aggregation, retaining the new session guard.** Its current result pagination can still scan the active resident population for a nearly empty result page, hold a snapshot for minutes, and repeat that work on the next periodic evaluation.

Use a bounded, ordered source batch/cursor and evaluate eligibility plus exact resident counts only for that batch. Advance the source cursor on empty eligible results and fully filtered batches, and finish only when the source is exhausted. Let read statements finish between batches so their snapshots are released; keep the session-level exclusion across the sweep. Preserve active-residency counting semantics, jurisdiction liveness, activation states, any undeleted membership exclusion, threshold resolution and ordinary activation calls. Do not weaken constitutional gates, introduce a whole-sweep transaction, disable clocks, change worker/Redis sizing, or replace exact counts with approximations.

Validate empty/mostly-filtered pages, cursor advancement, final partial batches, exact old/new candidate counts and crossings, concurrent changes under the existing sweep semantics, guard/reconnect/failure recovery, and later scheduled evaluation. Include a representative planner fixture proving bounded source work rather than relying on a final result LIMIT. If a different design is necessary to preserve semantics, explain that before claiming implementation or improvement.

Return one completed, tested, pushed handoff for manual relay. Demo will measure fresh windows with unchanged concurrency and retained bounded correctness checks. No bundled governor, judicial-nomination, audit or ledger optimization is requested in this exchange; those remain separate measured candidates.

Evidence: `/tmp/cga-d005-deploy/` — exact halt/stop/retirement records, configuration checksums, before/after service and worker state, baseline/post samples and calculations, guard samples, source-query plan, snapshot statistics, workload samples and `correctness-post/`. Existing phase monitoring remains in `/tmp/cga-remaining-phases-monitor/`.
