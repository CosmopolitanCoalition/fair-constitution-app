# D007 — operator-relayed Phase 9 evidence, 2026-09-20

This is a durable summary of the operator's complete D007 dispatch. The full raw
evidence remains on the demo box under `/tmp/cga-phase9-benchmark/`; the developer
has not accessed that box. The response and deployment instructions are in
`../SIMULATION_PHASE3_PERFORMANCE.md`. D006 was received separately during work
and is archived beside this file; its scan-rewrite candidate is not bundled here.

## Observed result (not a code-patch benchmark)

Demo revision `5a78d6a11c28db2a6c92122c7526fac7d196bcde`, run
`01a0ba29-3dbb-7181-8654-2d41ce1dea86`. Phase 9 `civics` began 06:16 UTC with
914,453 scopes, 0.1% population sample, 62% turnout and 73 workers. No code pull,
service restart, migration, worker-count change or independent-loop intervention.

The initial `SimBoardService::seatCgcGovernors()` join started with a global
organization-board prefix because board-type statistics still described only
departments. At 06:27, estimated table rows were 4.52 million, modifications
312,811, last autoanalyze 04:10:14: below the ordinary 10%-plus-50-row trigger.
Those are estimates, not table recounts.

Targeted `ANALYZE boards (boardable_type)` ran 06:28:11–06:28:21, 10.08 seconds,
with a one-second lock timeout and 30-second statement cap. This **manual**
maintenance updated planner statistics, not simulation records or application
code. It would not necessarily have happened at that time on an unattended run.

| Window | Direct completions | Scopes/hour |
|---|---:|---:|
| 06:24:46.880–06:28:10.904, stale statistics | 11,314 / 204.024s | 199,635 |
| 06:29:21.273–06:33:21.285, corrected statistics | 46,750 / 240.012s | 701,215 |

Measured recovery **3.51×**, after minute rates 701,388 / 719,614 / 678,319 /
705,540. Maintenance and the first minute after it are excluded. Sequential
workload/background changes limit exact causal attribution; query plans and
occupancy changed as predicted. Missing timed-out diagnostic samples were not
counted as zero; no read diagnostic timeout was raised.

| Nested timer per invocation | Before ms | After ms |
|---|---:|---:|
| Whole item | 1297.14 | 373.61 |
| Stage including final audit | 1255.54 | 356.44 |
| CGC charter plus seating | 1003.60 | 103.71 |
| Audit lock acquisition | 83.19 | 168.74 |
| Final audit batch | 92.28 | 173.75 |
| Claim acquisition | 29.25 | 5.12 |
| Scoped organization census | 1.32 | 0.41 |
| Bills | 23.28 | 8.62 |

Do not sum nested timings; batched sample counts differed slightly from direct
completion counts. Before, the plan estimated one organization board, scanned
`boards_one_per_body` by type alone and applied jurisdiction via organization PK
probes afterward. After, it used the organization jurisdiction index first and
both board index keys. The histogram then recognized organizations (sampled
fraction 2.56%). In 36 snapshots each side, board-query active sessions averaged
34.03 (max 52) before and 0.06 (max 1) afterward. Audit waiters then averaged
34.64; these observations are not CPU-time shares or proof another audit rewrite
would increase throughput.

## Requested developer work

1. Explicit scoped CGC IDs before board probes, host-bounded batches without a
   result cap, preserving boardable semantics rather than trusting `board_id`.
   Preserve inactive/non-deleted eligibility, vacancy-only seating, holder-pool
   precedence, terms/clocks and repeats. Check multi-board ordering honestly.
2. Add nested charter, governor-seating and board-lookup timings. Existing
   Phase9PerformanceTest mocked SimBoardService and missed this query.
3. Add narrow automatic statistics checks after new data arrives, with an
   explicit target list, persistent cooldown/evidence, single execution ownership,
   bounded host-sensitive budgets, automatic-maintenance conflict deferral and
   recovery after timeout/crash. Retain ordinary autovacuum/autoanalyze. No
   database-wide maintenance, per-lane dispatch, expensive counts, concurrency
   changes, durability relaxations or simulation-record changes.
4. Guarded disposable PostgreSQL tests must demonstrate stale-plan recovery,
   scoped index probes, exact eligible sets and seating outcomes, eligibility/
   skip cases, duplicate ownership, restart persistence, timeout/crash recovery,
   target restrictions and unchanged application outputs.
5. Return one completed tested/pushed developer handoff for manual operator
   relay. Keep D003/D004/D006 candidates separate. Do not claim a new live speedup
   for already-corrected statistics or reset production to create a comparison.

## Bounded correctness and limits

At 06:33:21: 114,073 / 914,453 completed, 800,380 remaining, 73 fresh leases,
zero review, no halt. Estimated remaining 68.5 minutes was conditional on that
window's workload and is not a current-state statement.

Separate 1,000-item samples reported 1,000 CGCs and 3,000 governors on each side.
Twenty scopes per side passed organization/bill counts, creation law/version and
adopted system-act links, 100% public stakes, public-domain IP records, 40 boards,
120 seated holders/terms, 60 expiry clocks, 40 threshold watchers, 60 bills and
60 sampled active workers. Each audit tail passed 129 hashes / 128 links.

These are bounded checks, not a planet replay. Worker-representative thresholds,
ordinary floor-ballot replay and endorsement associations were not validated.
Batched IP rows exposed `audit_seq=0`; tail integrity did not validate those
backreferences. No live write fixtures or full production test suite ran.

Redis stayed at its temporary 2 GiB cap, with data/persistence/eviction unchanged;
four pre-existing local configuration files and all service start times stayed
unchanged. No direct task messages were sent.
