# D009 — D008 deployed: Phase 10 throughput +14.8%, primary-key reads −99.8%

Completed 2026-09-20 UTC. One completed response for operator manual relay; no direct cross-task message sent. D008's developer response was received and implemented. Developer owns code/tests/commits; demo owns deployment/measurement. The independent Claude storage/resource/shutdown loop remains untouched.

## Result and disposition

Deployed **07fb6b0bf59edb0ae49c1f94bf69a2c406712c13**, from **c2ca186ffbfed30f42671a9cca2fd98640898906**, on the same Phase 10 run **01a0ba29-3dbb-7181-8654-2d41ce1dea86**. Retain the patch: measured throughput increased **14.80%**, and the targeted primary-key buffer-read cost fell **99.80% per new ledger row**. Monetary and chain checks passed on newly paid scopes.

| Condition | UTC interval | Direct completed scopes | Scopes/hour |
|---|---|---:|---:|
| Fresh baseline, c2ca186f | 08:22:40.883–08:25:40.892 | 6,681 / 180.009 s | **133,613** |
| Deployed, after startup allowance | 08:29:58.098–08:33:58.110 | 10,226 / 240.012 s | **153,382** |

Baseline minute rates: **133,728; 127,977; 139,134**. After minute rates: **151,836; 155,629; 155,827; 150,237**. All seven measured minutes retained **73 workers**, fresh leases, complete generation, zero review and no halt. Population sample remains 0.1%, turnout 62%, aspects elections/governance/civic_life/training/money. Both windows are still processing neighborhood stipend scopes.

New ledger rows per completed scope were **27.599 before / 27.553 after**, a closely matched write volume. Recipient selection remained about 12.9 ms per invocation. The repeatable throughput increase and specific index-read change support the intended mechanism. These are sequential live windows, not a randomized experiment; cache warmth and competing activity still limit exact causal attribution. There was no statistics maintenance, PostgreSQL/Redis restart, sizing change, index rebuild or historical-row rewrite during this comparison.

Latest snapshot **2026-09-20T08:35:10.571066+00:00**: **94,406 / 903,500 done**, **809,094 remaining**, 73 fresh leases, zero review. At the measured rate, remaining Phase 10 work projects to **5h17m**, compared with **6h03m** for the same remaining count at the fresh pre-patch rate: roughly **47 minutes saved**, if rates hold. This is an extrapolation, not a guarantee, and excludes Phase 11. The deployment pause itself was 2m08s.

## Deployment and review

Reviewed the complete committed D008 handoff, all four runtime diffs and focused test changes. Developer validation reports **32 tests / 1,868 assertions**. Demo PHP syntax checks passed for all four runtime files; the installed Laravel `Str::uuid7()` and ramsey/uuid **4.9.2** were verified with an in-memory ID generation, without database writes. No live fixture tests or application edits were performed on the demo.

The code generates UUIDv7 for new ledger row IDs before the lock and retains UUIDv4 posting-group IDs, the canonical payload, head read after lock, sequence-based chain order and existing transaction boundaries. The read-only LedgerEntry model now preserves both ID versions as strings. New payment timers are restricted to the open Step 5 stipend scope; ordinary payments retain their behavior.

- Halt requested **08:25:42.399**, drained **08:25:47.893**: zero running items, leases and pending counter deltas; zero review. Thirty upcoming scopes and their 412 wallet balances were captured while drained.
- Established `docker stop --time 120 fc_horizon` finished at **08:27:49.165**, exit **137** after its bound. This was not a graceful shutdown; simulation items had already drained. No orphaned old sweep execution remained to terminate.
- Fast-forwarded the exact reviewed revision, verified all four existing local configuration hashes, started Horizon, resumed the same run at **08:27:49.949**, and pumped it. Existing 903,500-item generation remained complete.
- Halt-to-resume **2m07.55s**. Scheduler, PostgreSQL and Redis retained their start times and container limits. Redis cap/data limit/persistence/eviction, worker count and all local configuration files were preserved. No migration, frontend build or scheduler refresh was needed.
- Post-deployment measurement began after at least two minutes of startup allowance, with 73 fresh leases and more than 1,000 new commit timing samples already flushed.

## Mechanism and remaining cost

Index buffer reads divided by new ledger inserts in the same windows:

| Index | Before reads/new row | After reads/new row |
|---|---:|---:|
| `ledger_entries_pkey` | 1.162742 | 0.002286 |
| `ledger_entries_account_type_account_id_index` | 0.708590 | 0.709408 |
| `ledger_public_account_seek_idx` | 0.744444 | 0.745404 |
| `ledger_entries_entry_group_index` | 0.100597 | 0.099749 |

Primary-key raw reads were **214,398 for 184,390 new rows** before versus **644 for 281,756 new rows** after. Both `n_tup_ins` deltas matched the ledger head sequence deltas in these windows. The two account-oriented indexes now dominate the measured ledger index reads: **199,880** and **210,022** reads in the after window. Their per-row costs stayed almost unchanged. Counters are database-wide buffer accesses, not physical-device bytes/latency or an isolated allocation of every read to one statement.

| Timer | Before mean ms/call | After mean ms/call | After flushed calls | After total ms/direct completion |
|---|---:|---:|---:|---:|
| `lane.item_total` | 1988.99 | 1716.81 | 10,315 | 1731.76 |
| `stipend.payment` | 2001.84 | 1729.00 | 10,059 | 1700.77 |
| `stipend.recipients` | 12.89 | 12.94 | 10,325 | 13.07 |
| `stipend.ledger_lock_wait` | not recorded | 852.45 | 20,168 | 1681.23 |
| `stipend.ledger_locked_post` | not recorded | 7.33 | 20,118 | 14.42 |
| `stipend.wallet_balances` | not recorded | 4.79 | 10,059 | 4.71 |
| `stipend.receipts` | not recorded | 1.03 | 10,059 | 1.02 |
| `stipend.commit` | not recorded | 2.07 | 10,059 | 2.04 |

Timers overlap. Payment includes its child intervals; `ledger_locked_post` excludes wallet/receipt work and the outer commit. A normal minted scope has two posting/lock invocations, with the second acquisition reentrant in the same transaction. Thus **852.45 ms per lock invocation must not be reported as the total per-scope wait**: the accumulated wait divided by direct completions is **1,681.23 ms**. Flush boundaries differ slightly among timers and direct completions; counts and per-completion attribution are preserved in raw evidence. No old-code Phase 10 detailed timer baseline exists.

Across **36 owner samples at five-second spacing**, 70–72 sessions still waited for the money-ledger key (mean 71.03); **24 sampled owners showed DataFileRead**, two WalSync and ten other/no wait states. These samples are not a measured time share. The new commit timer averages **2.07 ms**, while ledger post work totals **14.42 ms per direct completion** and wallet updates **4.71 ms**. The serialized path still governs throughput despite the primary-key improvement.

## Bounded correctness on payments that actually happened

Captured the next **30 pending stipend scopes** while halted, then let the normal resumed workers process them. All 30 completed and paid:

- **412 wallets actually changed**; every before/after balance difference equals the corresponding new account-ledger sum. All 412 new wallet credits match receipt amounts and credit direction/kind.
- **30 disbursements / 412 receipts** match stored item recipient counts and totals, unique recipient accounts and base-plus-bump arithmetic. The sampled path was minted, without short pay; this does not claim coverage of every policy or funding variant.
- **30 balanced stipend posting groups** match **30 distinct preceding issuance postings/events**, minted totals and ordinary civic-stipend reason. Issuance-to-payment chain boundaries match. This preserves the separate mint and payment groups under the existing outer transaction.
- All **854 sampled payment/issuance row hashes** recompute using the actual canonical-hash implementation. Their row IDs are UUIDv7; the 30 payment-group IDs are UUIDv4. Ten captured historical UUIDv4 rows retain their IDs and hashes.
- An additional **513-hash / 512-link ledger tail** and **129-hash / 128-link audit tail** pass. The audit tail mostly contains concurrent background activation/clock events; it is not per-payment event coverage.

No paid scopes were rerun and no production data was used as a write fixture. These are bounded checks, not a full ledger replay or exhaustive policy audit.

## One next bounded recommendation: assess the duplicate ledger account-index maintenance

Retain 07fb6b0b. Please audit whether the non-unique **`ledger_entries_account_type_account_id_index`** on `(account_type, account_id)` can be retired while retaining **`ledger_public_account_seek_idx`** on `(account_type, account_id, currency_id, seq)`. The latter has the same leading keys and is valid/ready on the demo. Both are maintained for every ledger row; the narrower index still costs **0.709 buffer reads per inserted row** after the UUID change.

This is a candidate, not a finding that every reader will perform equally on the wider index. A live planner-only check of the actual public treasury history query already uses the wider index with backward `seq` traversal and LIMIT 50. The narrower index had 104 recorded read scans at the metadata snapshot, versus 775 for the wider one; those are cumulative counters, not proof that no external reader depends on it. Audit all account-only and account/currency queries, administrative/reporting/import paths, index dependencies and migrations before deciding.

If that audit and disposable PostgreSQL fixtures establish safe query behavior, provide one migration that retires only the redundant non-unique index concurrently. Guard against unexpected definitions or a missing/invalid replacement; keep lock/execution budgets bounded and include retry/recovery plus a concurrent recreation path. Update fixtures to reflect the resulting production index set instead of silently continuing to test seven indexes after production has six. Verify unchanged results and bounded plans for both sparse economic accounts and a heavily populated treasury, mixed currencies and all relevant account-only history queries, alongside append-chain/rollback regressions. If the wider index imposes a material read regression or a dependency prevents removal, return that evidence and disposition rather than forcing a change.

Do not drop or disable an index on the live demo to test this hypothesis. No live index change was made in D009. Keep row/entry-group IDs, monetary postings, authority checks, transaction durability and concurrency unchanged. Actual benefit and any read-performance tradeoff must be measured after an approved completed developer handoff. The old D003/D004 candidates, surviving population-scan rewrite and infrastructure sizing remain separate.

## Evidence and delivery

Full evidence is in `/tmp/cga-d008-deploy/`: reviewed target and lint results, baseline/after samples and comparison, command log/drain/service checks, captured wallets and reconciliation, disbursements/receipts/issuance groups, recomputed hashes, chain tails, lock-owner samples, index metadata and planner-only public-history plan. A durable compact bundle is copied to `/home/cosmo/wos-step5-operations/evidence/D009/`.

D009 is complete and **awaiting operator manual relay**. No direct message was sent, and drafting this report is not recorded as delivery. The existing read-only phase monitor continues; the run remains active.
