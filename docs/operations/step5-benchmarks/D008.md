# D008 — D007 deployed at the natural phase boundary; Phase 10 payment benchmark

Completed 2026-09-20 UTC. Prepared for operator manual relay only. No direct cross-task message was sent. This acknowledges the developer's completed D007 response and supplies one next bounded Step 5 recommendation. The independent Claude storage/resource/shutdown loop is untouched.

## Deployment disposition

Deployed **c2ca186ffbfed30f42671a9cca2fd98640898906**, fast-forward from **5a78d6a11c28db2a6c92122c7526fac7d196bcde**, on run **01a0ba29-3dbb-7181-8654-2d41ce1dea86**. The incoming D007 runtime code, migration, tests and full committed handoff were reviewed. The developer reports **26 tests / 409 assertions** in disposable fixtures; the demo additionally syntax-checked the six runtime PHP files. No production write tests ran, and no application code was authored on the demo.

The operator asked to avoid interrupting the nearly finished phase. Live records showed courts had already completed at **06:16:00**; the ending phase was **9, civics**. All **914,453 civics items** completed naturally by **07:45:47.467**. Only then was the run halted for the deployment.

| Event | UTC / result |
|---|---|
| Halt requested / command returned | 07:45:47.478 / 07:45:47.847 |
| Drain verified | 07:45:47.904; zero running items, leases, pending counter deltas and review items |
| Scheduler stopped with established bound | `docker stop --time 60 fc_scheduler`; exited 137 after the bound |
| Horizon stopped with established bound | `docker stop --time 120 fc_horizon`; exited 137 after the bound |
| Old sweep database executions | None remained after stop; none needed termination |
| Additive metadata migration | `2026_09_20_073000_create_sim_statistics_maintenance.php`, completed 07:48:49; reported 61.17 ms |
| Horizon / scheduler started | 07:48:49.552 / 07:48:49.693 |
| Same run resumed | 07:48:50.149; halt-to-resume approximately **3m03s** |
| Phase 10 worklist generation finished | 07:50:09.587; **903,500** stipend scopes from **940,328** source identity items |

The stops used the established bounded procedure; they were **not graceful exits**. Both containers required the end-of-timeout kill, after simulation item drain. Queue generation took a further **79.44 seconds after resume**, separately from the deployment pause. The formal civics `finished_at` is 07:48:50 because phase advancement happened on resume; it must not be mistaken for ongoing civics work during the deployment.

Worker concurrency stayed **73**. PostgreSQL and Redis start times and container limits were unchanged. Redis retained the existing **2 GiB** runtime cap and existing data-limit/persistence/eviction configuration. All four pre-existing modified local configuration files passed unchanged SHA-256 checks. No frontend build, database restart, sizing change, simulation reset or manual ANALYZE occurred in this deployment.

## D007 validation and comparison limits

The new scheduler job was consumed each minute. Its metadata repeatedly reports **`no_active_civics_run`**, with no attempt and no success recorded. At **07:59:01**, this remained correct: Phase 9 had ended. This verifies scheduling and the skip guard; it does **not** establish a live automatic ANALYZE, cooldown/crash recovery execution, or new maintenance speedup on this run. Those paths retain the developer's fixture evidence. Historical manual statistics maintenance and its measured 3.51× Phase 9 recovery remain separate.

The new private scoped CGC lookup was called read-only for **20 previously checked jurisdictions**, yielding the same 20 board IDs. Captured SQL was scoped to the explicit organization IDs and organization type, with the intended status and soft-delete filters. No seating method or other write path was invoked. This verifies the bounded live read result, not a replay of institutional writes.

Because deployment followed natural Phase 9 completion, **there is no live old-code/new-code Phase 9 throughput comparison**. Phase 10 uses a different workload and its payment implementation was not changed by c2ca186f. The following measurements are an absolute Phase 10 baseline, not evidence of D007 speeding up or slowing down payments.

## Completed Phase 10 measurement

Population sample **0.1%**, turnout **62%**, aspects **elections, governance, civic_life, training, money**. Generation was complete; every endpoint below had 73 fresh leases, no halt and zero review items. Generation and initial worker startup are excluded. Rates are still improving, so capture a fresh baseline immediately before any next deployment rather than treating the earliest minutes as stationary.

| UTC interval | Direct completions | Scopes/hour |
|---|---:|---:|
| 07:53:30–07:54:30 | 1,895 | 113,698 |
| 07:54:30–07:55:30 | 1,929 | 115,743 |
| 07:55:30–07:56:30 | 2,187 | 131,223 |
| 07:56:30–07:57:30 | 2,206 | 132,360 |
| 07:57:30–07:58:30 | 2,144 | 128,638 |
| **07:53:30–07:58:30 combined** | **10,361 / 299.999 seconds** | **124,332** |

The earlier 07:51:12–07:55:12 window completed 7,149 scopes at **107,236/hour**. That window caught the initial timing flush and continuing warmup, so the five-minute window above is the preferred baseline. Its most recent minute was **128,638/hour**, after a prior minute at **132,360/hour**. No interventions occurred between these measurements.

| Timer | Flushed samples in preferred window | Mean per invocation |
|---|---:|---:|
| `lane.item_total` | 10,950 | 2,144.48 ms |
| `stage.stipend_scope` | 10,950 | 2,133.21 ms |
| `stipend.payment` | 10,707 | 2,163.13 ms |
| `stipend.recipients` | 10,950 | 15.03 ms |
| `stipend.office_holders` | 10,707 | 1.25 ms |
| `stipend.settings` | 10,707 | 1.73 ms |
| `lane.claim_next` | 10,950 | 3.25 ms |
| `lane.settle` | 10,950 | 1.72 ms |

Timers overlap and must not be summed. Payment has fewer calls than scopes because some scopes do not pay. Batched timing flushes yielded 10,950 full-item samples versus 10,361 direct completions; they are not identical cohorts. Of the accumulated stage time in this window, approximately **99% was inside `stipend.payment`**. This includes waiting, work and the durable commit. The existing detailed `training.ledger_*` timers only run during training and therefore provide **no Phase 10 lock-duration measurement**.

Latest completed health snapshot **2026-09-20T07:59:20.692834+00:00**: **17,282 / 903,500** stipend scopes done, **73 fresh leases**, **zero review**, run active. At the preferred window's rate, the remaining work is about **7.1 hours**; this is a provisional extrapolation of the current workload, not a completion guarantee.

## Measured hotspot: database page reads while holding the money-ledger lock

A corrected 36-snapshot, five-second sampler ran **07:52:14.615–07:55:11.856**. It matched the actual ledger advisory key **0x4c45444752**, PostgreSQL lock fields classid **76**, objid **1162102610**. In every snapshot, **70–72 sessions** waited for this lock; mean **71.03**. These are queue occupancy observations, not a substitute for per-scope wait timers.

Of 36 sampled owners, **29 showed `DataFileRead`**, two `DataFileWrite`, two `WalSync`, and three other/no wait states. **22 owner samples** were in the ledger append/treasury CTE with `DataFileRead`; five were wallet balance updates with `DataFileRead`. Only one sampled owner was executing `COMMIT`. Owner observations are discrete samples, not an exact percentage attribution of lock-held time. They nevertheless point toward page access inside the serialized write path, rather than assuming the earlier training COMMIT pattern is still dominant.

A separate **45.818-second** cumulative-statistics delta, **07:57:03.051–07:57:48.869**, showed these ledger index buffer reads:

| Ledger index | Buffer reads added |
|---|---:|
| `ledger_entries_pkey` (UUID id) | **51,043** |
| `ledger_public_account_seek_idx` | 34,073 |
| `ledger_entries_account_type_account_id_index` | 32,009 |
| `ledger_entries_entry_group_index` | 4,464 |
| `ledger_currency_seek_idx` | 55 |
| `ledger_entries_seq_unique` | 29 |
| `ledger_entries_created_at_index` | 7 |

These counters are database-wide and include any other activity using those indexes; they are buffer reads, **not measured physical-storage bytes or latency**. `track_io_timing` remains off. The primary-key index was estimated at about **855 MiB** from catalog pages, against unchanged **512 MiB shared_buffers**. No infrastructure setting was adjusted. `LedgerService::post` currently creates every row id using random **`Str::uuid()`**; the row id is outside the canonical hash payload, while `entry_group` is inside it. This is a concrete candidate for avoidable scattered index writes/reads, not yet proof of the size of a possible fix.

## One next bounded recommendation: improve new ledger-row insertion locality

Please investigate and, if the compatibility checks support it, implement **time-ordered UUIDs for newly created ledger row IDs in `LedgerService::post`**, using the repository-supported PostgreSQL-compatible UUID implementation. Keep this pass limited to the ledger row ID and the Phase 10 measurements needed to evaluate it. Retain the existing entry-group generation, canonical payload, head-read-after-lock rule, sequence order, global lock, two posting groups, wallet/receipt writes and outer durable transaction. Do not rewrite existing rows or hashes, drop indexes, relax durability, change monetary policy/sample size, or increase concurrency to mask the serialization.

The specific hypothesis is fewer primary-key leaf pages touched while the global lock is held. Audit UUID-version assumptions, references/imports/serialization, clock behavior and concurrent generation before changing identifiers. If an identifier contract prevents this, return the reason rather than manufacturing a patch. The account-oriented ledger indexes will still require work, so **no whole-phase speedup factor is established**, and the change may recover only part of the measured cost.

Alongside this one optimization, enable narrowly scoped Phase 10 equivalents of the existing ledger lock-wait, post-after-lock and wallet-update timers, plus receipt insertion and owned-commit timing. Preserve ordinary payment behavior and avoid double-counting nested intervals. This will distinguish reduced insertion cost from unrelated cache/workload changes; the existing outer `stipend.payment` timer cannot do that alone.

Validate in guarded disposable PostgreSQL: production index definitions and a representative pre-existing UUID population; concurrent new IDs and appenders; exact canonical hashes and links for fixed monetary payloads; conserved amounts; wallet/receipt totals; rollback after mint/credit failures; outer-caller rollback; append-only protections. Local buffer/plan evidence is useful but is not a live throughput claim. Return one tested, pushed handoff with deployment requirements. The demo will capture a fresh baseline, deploy via the bounded drain/refresh procedure, then compare several direct-completion windows and index-read deltas on the same Phase 10 run. No restart of a completed phase is authorized to manufacture a comparison.

## Bounded correctness

- **20 newly completed Phase 10 scopes**, 20 minted disbursements and **276 receipts**: stored metrics agree with disbursement recipient counts/totals; receipts sum to the disbursement totals; account IDs are unique within each disbursement; the sampled non-short-paid receipts equal base plus bump. No mismatches; no short-pay case sampled. This does not independently re-resolve every policy or prove every recipient's eligibility/order.
- A **513-hash / 512-link ledger tail** and **129-hash / 128-link audit tail** passed. The audit sample mostly covers concurrent background activation events, so it is a chain-integrity check, not per-payment event coverage.
- **272 wallets captured before Phase 10** reconciled to subsequent account-ledger deltas with zero mismatches, but **none had yet changed** at the final 07:59:20 check. This is explicitly **not evidence of validated newly paid wallet deltas**. The completed disbursement/receipt checks above are the live payment evidence.
- All health endpoints retained zero review and fresh workers. No production write fixtures or reruns of paid scopes were used.

## Evidence and exchange state

Full raw evidence: `/tmp/cga-d007-deploy/`. Key files: `commands.jsonl`, `boundary.json`, `drained.json`, `migration.json`, `resumed.json`, service snapshots, configuration hashes, `scoped-lookup-validation.json`, `steady-benchmark.json`, `steady-samples.json`, `activity.jsonl`, `io-deltas.json`, `payment-validation.json`, `wallet-final-validation.json`, ledger/audit tails and `final-health.json`.

The preliminary file `activity-initial-invalid-owner-label.jsonl` had an incorrect owner-key label and is **excluded** from these findings; the corrected 36-sample file above is the source. Diagnostic samplers are read-only. The existing minute-by-minute phase monitor continues.

A compact evidence bundle is retained beside this report under `evidence/D008/`. D008 is complete and awaiting operator manual relay. This document is not proof of delivery, and no second developer exchange has been started.
