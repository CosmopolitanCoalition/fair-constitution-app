<?php

namespace App\Http\Controllers\Civic;

use App\Http\Controllers\Controller;

/**
 * FE-C10 ROUTE ANCHOR — Relocation (PHASE_C_DESIGN_frontend.md §B.14).
 * Skeleton so the up-front Phase C route table resolves; REPLACE with
 * the real controller in the batch-3 WI (FE-C10).
 */
class RelocationController extends Controller
{
    public function show()
    {
        abort(501, 'Relocation surface lands with FE-C10 (Phase C batch 3).');
    }

    public function travelling()
    {
        abort(501, 'Travelling reset lands with FE-C10 (Phase C batch 3).');
    }
}
