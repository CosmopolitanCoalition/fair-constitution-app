<?php

namespace App\Http\Controllers\System;

use App\Http\Controllers\Controller;

/**
 * FE-C10 ROUTE ANCHOR — Term sync (PHASE_C_DESIGN_frontend.md §B.16).
 * Skeleton so the up-front Phase C route table resolves; REPLACE with
 * the real controller in the batch-3 WI (FE-C10).
 */
class TermSyncController extends Controller
{
    public function show()
    {
        abort(501, 'Term-sync surface lands with FE-C10 (Phase C batch 3).');
    }
}
